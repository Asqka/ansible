
- name: Create user newadmin
  hosts: NGINX
  become: yes

  tasks:

    - name: Add user newadmin
      user:
        name: newadmin
        groups: sudo
        append: yes
        shell: /bin/bash

    - name: Set password for newadmin
      shell: echo "newadmin:123" | chpasswd
