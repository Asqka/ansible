
- name: Deploy application from Git
  hosts: NGINX
  become: yes

  tasks:

    - name: Install git
      apt:
        name: git
        state: present
        update_cache: yes

    - name: Clone repository
      git:
        repo: https://github.com/octocat/Hello-World.git
        dest: /opt/app
        force: yes
